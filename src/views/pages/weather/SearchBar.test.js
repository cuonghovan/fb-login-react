import React from 'react';
import { mount } from 'enzyme';
import SearchBar from './SearchBar';

describe('SearchBar', () => {
  it('triggers successful fetch weather info action when enters correct location', () => {
    const fetchWeatherInfoMock = jest.func  
  });
});
